import { Data } from '../interfaces/Ordenes';
import { useLocation } from "react-router-dom";

// const location = useLocation();
// const user = location.state?.user;
// const token: string = user.data.token


export let rows: Data[] = [  
];